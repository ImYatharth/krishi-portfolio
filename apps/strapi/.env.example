# Server
HOST=0.0.0.0
PORT=1337

# Secrets - generate unique random strings for each of these in production
APP_KEYS=tobechanged1,tobechanged2,tobechanged3,tobechanged4
API_TOKEN_SALT=tobechanged
ADMIN_JWT_SECRET=tobechanged
TRANSFER_TOKEN_SALT=tobechanged
JWT_SECRET=tobechanged
ENCRYPTION_KEY=tobechanged

# Database (SQLite for local dev, Postgres for production)
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db
# DATABASE_CLIENT=postgres
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Public URL of this Strapi instance (for CORS on Railway)
# STRAPI_PUBLIC_URL=https://cms.yourdomain.com
